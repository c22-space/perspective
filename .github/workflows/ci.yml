name: CI

on:
  pull_request:
    branches: [main, implementation]

env:
  CARGO_TERM_COLOR: always

jobs:
  # fmt:
  #   name: Format
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-toolchain@stable
  #       with:
  #         components: rustfmt
  #     - run: cargo fmt --all -- --check

  # clippy:
  #   name: Clippy
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-toolchain@stable
  #       with:
  #         components: clippy
  #     - uses: Swatinem/rust-cache@v2
  #     - run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace

  # build-python:
  #   name: Build Python bindings
  #   runs-on: ubuntu-latest
  #   needs: [fmt, clippy, test]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2
  #       with:
  #         key: python
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"
  #     - run: pip install maturin
  #     - name: Build wheel
  #       working-directory: crates/perspective-python
  #       run: maturin build --release
  #     - name: Install wheel
  #       run: pip install target/wheels/*.whl
  #     - name: Smoke test
  #       run: |
  #         python3 -c "
  #         from perspective_python import PerspectiveEngine
  #         import tempfile
  #         d = tempfile.mkdtemp()
  #         e = PerspectiveEngine(d, dashboard_port=None)
  #         id1 = e.store('test', 'hello world', 'episodic')
  #         assert id1, 'store returned empty id'
  #         results = e.recall('test', 'hello', budget=5)
  #         assert len(results) > 0, 'recall returned no results'
  #         e.delete('test', id1)
  #         print('All smoke tests passed')
  #         "
